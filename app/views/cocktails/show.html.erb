  <div class="container">
    <h1><%= @cocktail.name %></h1>
    <%= link_to "Add a little description", new_cocktail_dose_path(@cocktail), class: "btn btn-success" %>
  </div>

<!-- AFFICHER MES PLANTES -->

<br>

<div class="container">
  <div class="cards">
  <% @cocktail.doses.each do |dose| %>
  <%= dose.description %>
  <%= dose.ingredient.name %>
  <%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>
  <%= link_to "Delete", dose_path(dose), method: :delete, class: "btn btn-small btn-danger" %>
    <% end %>
  </div>
  <%= link_to "See All Cocktails", cocktails_path, class: "btn btn-small btn-primary" %>
</div>
